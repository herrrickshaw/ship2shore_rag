name: quality-gates

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gitleaks:
    name: secrets (working tree)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install gitleaks
        run: |
          curl -fsSL https://github.com/gitleaks/gitleaks/releases/download/v8.30.1/gitleaks_8.30.1_linux_x64.tar.gz \
            | tar -xz gitleaks
          sudo mv gitleaks /usr/local/bin/gitleaks
      - name: Scan working tree for secrets
        run: gitleaks detect --no-git --redact --no-banner --source .

  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.11", cache: pip }
      - run: pip install pytest pyyaml sqlite-vec beautifulsoup4 python-docx openpyxl python-pptx pypdf requests "psycopg[binary]" pgvector fastapi httpx
      - name: Run tests (no live Postgres / no heavy ML deps needed for these)
        run: pytest -q tests/test_chunk.py tests/test_sources.py tests/test_sqlite_store.py tests/test_loaders.py tests/test_export.py tests/test_ops_auth.py tests/test_ops_store.py tests/test_api.py tests/test_cite_check.py tests/test_diversify.py tests/test_query_log.py tests/test_retriever_filters.py tests/test_freshness.py tests/test_webui.py
