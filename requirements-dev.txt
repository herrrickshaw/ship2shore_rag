# Dev-only tooling: linting, formatting, coverage. Not needed to run the
# app itself (see requirements.txt / requirements-api.txt / requirements-
# warehouse.txt for that) -- same scoped-requirements-file convention used
# across this account's repos (global-market-data, global-stock-screener).
ruff>=0.16
black>=26.0
pre-commit>=4.0
pytest-cov>=6.0
