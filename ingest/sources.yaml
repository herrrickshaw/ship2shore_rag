# Curate additional PDF/URL literature here. Only add sources you have the
# right to ingest (public domain, freely licensed, or your own documents).
#
# Entries are PDFs by default (fetched with pypdf, entirely local). Add
# `type: html` for a non-PDF web page instead — those are fetched as clean
# markdown via Jina AI Reader (r.jina.ai, free tier, no API key) rather than
# raw HTML scraping, future-proofing this file for port authority pages,
# IMO circulars, and industry white papers that aren't PDFs. That routes
# the URL through Jina's servers, which is fine for public pages but NOT
# appropriate for anything under restricted redistribution terms — use
# `--source file` for those instead, which never leaves this machine.
#
# UNCTAD's annual "Review of Maritime Transport" is published openly:
# https://unctad.org/publication/review-maritime-transport-2024
# Add the direct PDF link(s) you want ingested below.
#
# NTSB marine accident reports are U.S. government works (17 U.S.C. §105 —
# public domain). `cli.py ingest --source ntsb` auto-discovers these via
# CAROL's unauthenticated JSON API (see ingest/sources.py:fetch_ntsb and
# README "Sources") rather than needing them hand-curated here — the couple
# below are kept only as a small fallback set, not the primary path anymore.
#
# DNV class rules (RU-SHIP) are free and public at rules.dnv.com, but
# distributed only as whole-edition ZIP archives (hundreds of PDFs each), not
# individually crawlable chapter links — see:
# https://rules.dnv.com/servicedocuments/dnvpm/packages?category=rulesship
# To use a specific chapter: download the edition ZIP yourself, extract it,
# then ingest the PDFs you want with `cli.py ingest --source file --path`
# rather than adding them here.
#
# Some formats genuinely have no public source to crawl at all — they're
# per-vessel/per-company private documents by nature, not published
# literature: PMS (planned maintenance system) technical manuals, SIRE 2.0 /
# CDI vetting inspection reports, and statutory certificates (SOLAS/MARPOL/
# ISM/ISPS). Ingest your own copies of these via `--source file` — there is
# no fetcher for them here because there is nothing public to fetch.

pdf_sources:
  - url: "https://www.ntsb.gov/investigations/AccidentReports/Reports/MIR2540.pdf"
    title: "Contact of Containership Dali with the Francis Scott Key Bridge"
    license: "U.S. government work — public domain"
  - url: "https://www.ntsb.gov/investigations/AccidentReports/Reports/MIR2513.pdf"
    title: "Diesel Generator Engine Failure Aboard Passenger Vessel Ocean Navigator"
    license: "U.S. government work — public domain"
  - url: "https://www.ntsb.gov/investigations/AccidentReports/Reports/MIR2521.pdf"
    title: "Capsizing and Sinking of Towing Vessel Baylor J. Tregre"
    license: "U.S. government work — public domain"
#  - url: "https://unctad.org/system/files/official-document/rmt2024_en.pdf"
#    title: "UNCTAD Review of Maritime Transport 2024"
#    license: "UNCTAD open access"

  # Specific arXiv IDs found by name (not via the seed-query search) — added
  # as direct PDF pulls rather than through --source arxiv since that goes
  # through the arXiv API search, not exact-ID lookup.
  - url: "https://arxiv.org/pdf/2505.07553"
    title: "Towards Requirements Engineering for RAG Systems"
    license: "arXiv non-exclusive license"
  - url: "https://arxiv.org/pdf/2606.29721"
    title: "Redefining Maritime Anomaly Detection via Equation-Grounded Synthetic Anomalies"
    license: "arXiv non-exclusive license"
  - url: "https://arxiv.org/pdf/2603.04423"
    title: "Generating Realistic, Protocol-Compliant Maritime Radio Dialogues using Self-Instruct and Low-Rank Adaptation"
    license: "arXiv non-exclusive license"
  - url: "https://arxiv.org/pdf/2608.21036"
    title: "Evaluating Large Language Model Performance on International Maritime Dangerous Goods Code Compliance"
    license: "arXiv non-exclusive license"
  - url: "https://arxiv.org/pdf/2605.15937"
    title: "A Retrieval-Enhanced Transformer for Multi-Step Port-of-Call Sequence Prediction in Global Liner Shipping"
    license: "arXiv non-exclusive license"
  - url: "https://arxiv.org/pdf/2605.18408"
    title: "Historical Knowledge Graphs for Global Maritime Estimated Time of Arrival"
    license: "arXiv non-exclusive license"
  # https://www.mdpi.com/1424-8220/26/4/1227 — 403'd on fetch (couldn't
  # verify title/license), skipped rather than guessed.

  # type: html example — not a PDF, fetched via Jina Reader (see header
  # comment above). Distinct from the "MARPOL" article already in
  # DEFAULT_WIKIPEDIA_TITLES (ingest/sources.py) — this is genuinely new
  # content, not a duplicate.
  - url: "https://en.wikipedia.org/wiki/Maritime_Labour_Convention"
    title: "Maritime Labour Convention"
    license: "CC BY-SA 4.0"
    type: html
