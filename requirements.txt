psycopg[binary]>=3.1
pgvector>=0.2
sqlite-vec>=0.1  # vessel-side backend: single-file, no server — see README "Shipboard deployment"
sentence-transformers>=2.7
requests>=2.31
pypdf>=4.2
cryptography>=42.0  # pypdf needs this for AES-encrypted PDFs (some gov't reports restrict editing)
beautifulsoup4>=4.12
python-docx>=1.1  # local .docx ingestion (ingest/loaders.py)
openpyxl>=3.1     # local .xlsx ingestion (ingest/loaders.py)
python-pptx>=1.0  # local .pptx ingestion (ingest/loaders.py)
pyyaml>=6.0
anthropic>=0.34  # optional: only used by `ask` when ANTHROPIC_API_KEY is set
fastapi>=0.115   # api.py — REST wrapper around ops/store.py
uvicorn>=0.32
pytest>=8.0
